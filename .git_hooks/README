= Overview =

.git_hooks/ replaces .git/hooks/ so that hooks may be tracked as part of the repository. Create local untracked hooks in .git_hooks/local/ and source run_local_hooks in each hook script to run the local hook.

= Setup =

  ./bin/setup

= TODO =

* Add useful hooks
